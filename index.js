let divide = (a, b) => {
  let reuslt = a / b;
  return reuslt;
}
let square = n => n * 2;
  alert( square(2 ));

  const add = (a,b) => {
    console.log(`Adding ${a}`);
    console.log(`Adding ${b}`);
    return a + b;
  }
  